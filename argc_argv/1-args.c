#include <stdio.h>
/**
 * main - prints the number of the strings in line
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: Always 0 (Success)
 */


int main(int argc,char* argv[])
{
    (void)argv;
    printf("\n%d",argc-1);
    return(0);
}